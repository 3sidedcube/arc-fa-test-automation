package com.cube.qa.framework.testdata.model;

/**
 * One answer row inside a {@link QuizQuestion}. Title is the user-visible
 * label (used both to identify the answer in the bundle and to tap the
 * matching button in the app).
 */
public class QuizAnswer {

    public String titleEn;
    public boolean isCorrect;
}
